## src/paic/analytics/io.py
111:         relative_path = Path("tables") / f"{table_name}.parquet"
127:                 byte_size=path.stat().st_size,
167: @artifact_reader
169:     path = Path(analytics_dir) / "manifest.json"
171:         return AnalyticsManifest.model_validate_json(path.read_text(encoding="utf-8"))
186: @artifact_reader
188:     root = Path(analytics_dir)

## src/paic/analytics/validation.py
72: @artifact_readers("analytics_dir", "dataset_dir")
78:     root = Path(analytics_dir)
93:         expected_marker = hashlib.sha256(manifest_path.read_bytes()).hexdigest()
94:         marker = marker_path.read_text(encoding="utf-8").strip()
102:         config = AnalyticsConfig.model_validate_json(config_path.read_text(encoding="utf-8"))
122:         catalog = json.loads(catalog_path.read_text(encoding="utf-8"))
145:             if path.stat().st_size != item.byte_size:
232:         source_root = Path(dataset_dir)

## src/paic/detection/io.py
96:         relative_path = Path("tables") / f"{table_name}.parquet"
112:                 byte_size=path.stat().st_size,
170:     path = Path(detection_dir) / "manifest.json"
172:         return DetectionManifest.model_validate_json(path.read_text(encoding="utf-8"))
187: @artifact_reader
189:     root = Path(detection_dir)

## src/paic/detection/validation.py
148: @artifact_readers("detection_dir", "analytics_dir")
154:     root = Path(detection_dir)
167:     elif marker.read_text(encoding="utf-8").strip() != file_sha256(manifest_path):
178:             config = DetectionConfig.model_validate_json(config_path.read_text(encoding="utf-8"))
203:         if path.stat().st_size != table_manifest.byte_size:
279:         analytics_root = Path(analytics_dir)

## src/paic/evaluation/artifact.py
145:     with path.open("rb") as handle:
152:     descriptor = os.open(path, os.O_RDONLY)
161:     descriptor = os.open(path, flags, 0o600)
172:     if root.is_symlink() or not root.is_dir():
174:     entries = list(root.iterdir())
178:     if any(entry.is_symlink() or not entry.is_file() for entry in entries):
297:                     byte_size=(staging / name).stat().st_size,
329:     return json.loads(path.read_text(encoding="utf-8"))
333:     return [json.loads(line) for line in path.read_text(encoding="utf-8").splitlines() if line]
336: @artifact_reader
338:     target = Path(root)
342:             (target / "manifest.json").read_text(encoding="utf-8")
346:         if _sha(target / "manifest.json") != (target / "_SUCCESS").read_text().strip():
352:             if path.is_symlink() or not path.is_file():
354:             if path.stat().st_size != item.byte_size or _sha(path) != item.sha256:
418: @artifact_readers("root", "visible_dir", "answers_dir")
433:     calibration = _read_json(Path(root) / "calibration.json")
434:     safety = SafetyResults.model_validate(_read_json(Path(root) / "safety-results.json"))
452:     config = EvaluationConfig.model_validate_json(Path(config_path).read_text(encoding="utf-8"))

## src/paic/evaluation/comparison.py
104: @artifact_readers("left_dir", "right_dir", "visible_dir", "answers_dir")
215:     with path.open("xb") as handle:
222:     descriptor = os.open(path, os.O_RDONLY)
254: @artifact_reader
256:     path = Path(root)
257:     if path.is_symlink() or not path.is_dir():
259:     entries = list(path.iterdir())
262:     if any(item.is_symlink() or not item.is_file() for item in entries):
265:         manifest_bytes = (path / "manifest.json").read_bytes()
266:         marker = (path / "_SUCCESS").read_text(encoding="utf-8").strip()
270:         comparison_bytes = (path / "comparison.json").read_bytes()
286: @artifact_readers("comparison_dir", "left_dir", "right_dir", "visible_dir", "answers_dir")

## src/paic/evidence/io.py
95:         relative = Path("tables") / f"{name}.parquet"
111:                 byte_size=path.stat().st_size,
154:     path = Path(evidence_dir) / "manifest.json"
156:         return EvidenceManifest.model_validate_json(path.read_text(encoding="utf-8"))
171: @artifact_reader
173:     root = Path(evidence_dir)

## src/paic/evidence/validation.py
44:     manifest = Path(path) / "manifest.json"
177: @artifact_readers("evidence_dir", "dataset_dir", "analytics_dir", "detection_dir", "impact_dir")
186:     root = Path(evidence_dir)
226:         if path.stat().st_size != meta.byte_size or frame.height != meta.row_count:
238:         config = EvidenceConfig.model_validate_json(config_path.read_text(encoding="utf-8"))
250:     if not marker.is_file() or marker.read_text(encoding="utf-8").strip() != file_sha256(

## src/paic/impact/io.py
89:         relative = Path("tables") / f"{table_name}.parquet"
105:                 byte_size=path.stat().st_size,
145:     path = Path(impact_dir) / "manifest.json"
147:         return ImpactManifest.model_validate_json(path.read_text(encoding="utf-8"))
162: @artifact_reader
164:     root = Path(impact_dir)

## src/paic/impact/validation.py
33: @artifact_readers("impact_dir", "dataset_dir")
37:     root = Path(impact_dir)
56:         if path.stat().st_size != meta.byte_size or frame.height != meta.row_count:
64:         config = ImpactConfig.model_validate_json(config_path.read_text(encoding="utf-8"))
72:     if not marker.is_file() or marker.read_text(encoding="utf-8").strip() != file_sha256(
77:         source_manifest = Path(dataset_dir) / "manifest.json"

## src/paic/investigation/artifact.py
100:                 byte_size=path.stat().st_size,
125:     if Path(relative).name != relative or relative in {"", ".", ".."}:
148:         if not root.is_dir() or root.is_symlink():
150:         entries = list(root.iterdir())
157:         if entry.is_symlink():
164: @artifact_reader
166:     root = Path(path)
172:             (root / "manifest.json").read_text(encoding="utf-8")
175:             (root / "investigation.config.resolved.json").read_text(encoding="utf-8")
178:             (root / "report.json").read_text(encoding="utf-8")
180:         request_receipt = json.loads((root / "request.receipt.json").read_text(encoding="utf-8"))
183:             for line in (root / "transcript.jsonl").read_text(encoding="utf-8").splitlines()
268: @artifact_readers(
280:     root = Path(path)
304:         if target.stat().st_size != item.byte_size or file_sha256(target) != item.sha256:
308:     if not marker.is_file() or marker.read_text(encoding="utf-8").strip() != file_sha256(
488: @artifact_readers(

## src/paic/recovery/artifact.py
44:     with path.open("rb") as handle:
50: @artifact_reader
52:     return file_sha256(Path(path) / "manifest.json")
56:     with path.open("x", encoding="utf-8") as handle:
63:         byte_size=path.stat().st_size,
109:             with manifest_path.open("x", encoding="utf-8") as handle:
115:             with marker.open("x", encoding="utf-8") as handle:
128:     if not root.is_dir() or root.is_symlink():
130:     entries = list(root.iterdir())
135:     if any(item.is_symlink() or not item.is_file() for item in entries):
139:             (root / "manifest.json").read_text(encoding="utf-8")
148:         if target.stat().st_size != item.byte_size or file_sha256(target) != item.sha256:
152:     if (root / "_SUCCESS").read_text(encoding="utf-8").strip() != file_sha256(
159: @artifact_reader
161:     root = Path(path)
165:             (root / "recovery.config.resolved.json").read_text(encoding="utf-8")
168:             (root / "observation-set.json").read_text(encoding="utf-8")
171:             (root / "report.json").read_text(encoding="utf-8")
181:         for line in (root / "metric-evaluations.jsonl").read_text(encoding="utf-8").splitlines()
214: @artifact_readers("path", "observations_dir", "analytics_dir", "execution_dir")

## src/paic/recovery/observations.py
71:     with path.open("x", encoding="utf-8") as handle:
78: @artifact_readers("analytics_dir", "execution_dir")
141:             "analytics_manifest_sha256": file_sha256(Path(analytics_dir) / "manifest.json"),
181:                         byte_size=(staged / name).stat().st_size,
196: @artifact_readers("path", "analytics_dir", "execution_dir")
203:     root = Path(path)
204:     if root.is_symlink() or not root.is_dir() or {item.name for item in root.iterdir()} != EXPECTED:
206:     if any(item.is_symlink() or not item.is_file() for item in root.iterdir()):
210:             (root / "manifest.json").read_text(encoding="utf-8")
214:     if (root / "_SUCCESS").read_text(encoding="utf-8").strip() != file_sha256(
220:         if target.stat().st_size != item.byte_size or file_sha256(target) != item.sha256:
223:         (root / "observation.config.resolved.json").read_text(encoding="utf-8")
226:         (root / "observation-set.json").read_text(encoding="utf-8")
259:             Path(analytics_dir) / "manifest.json"
281: @artifact_reader
283:     return file_sha256(Path(path) / "manifest.json")

## src/paic/remediation/artifact.py
98:                 byte_size=path.stat().st_size,
209:     if Path(relative).name != relative or relative in {"", ".", ".."}:
220:         if not root.is_dir() or root.is_symlink():
222:         entries = list(root.iterdir())
230:         if entry.is_symlink():
243:             (root / "manifest.json").read_text(encoding="utf-8")
252:         if not target.is_file() or target.is_symlink():
254:         if target.stat().st_size != item.byte_size or file_sha256(target) != item.sha256:
258:     if marker.read_text(encoding="utf-8").strip() != file_sha256(manifest_path):
263: @artifact_reader
265:     root = Path(path)
270:         state = ControlState.model_validate_json((root / "state.json").read_text(encoding="utf-8"))
282: @artifact_reader
284:     root = Path(path)
291:             (root / "remediation.config.resolved.json").read_text(encoding="utf-8")
294:             (root / "proposal.json").read_text(encoding="utf-8")
296:         plan = RemediationPlan.model_validate_json((root / "plan.json").read_text(encoding="utf-8"))
321: @artifact_reader
323:     root = Path(path)
329:             (root / "receipt.json").read_text(encoding="utf-8")
354: @artifact_reader
356:     return str(file_sha256(Path(path) / "manifest.json"))
367: @artifact_readers(
438: @artifact_readers("path", "plan_dir", "before_state_dir", "after_state_dir")

## src/paic/simulator/io.py
46:     with path.open("rb") as handle:
95:         relative_path = Path("tables") / f"{table_name}.parquet"
122:                 byte_size=path.stat().st_size,
167: @artifact_reader
169:     path = Path(dataset_dir) / "manifest.json"
171:         return DatasetManifest.model_validate_json(path.read_text(encoding="utf-8"))
186: @artifact_reader
188:     root = Path(dataset_dir)

## src/paic/simulator/validation.py
93: @artifact_reader
97:     root = Path(dataset_dir)
119:         config = SimulationConfig.model_validate_json(config_path.read_text(encoding="utf-8"))
159:             if path.stat().st_size != table_manifest.byte_size:
202:         value = marker.read_text(encoding="utf-8").strip()

