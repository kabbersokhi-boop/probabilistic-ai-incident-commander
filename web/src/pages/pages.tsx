import type { Bundle, RecordValue } from '../bundle/schema'; import { unavailable, asRows } from '../bundle/schema'; import { currency, integer, number, percent, score, time } from '../bundle/formatters'; import { hypotheses, impact, lead, plan, receipt, recovery, report, rows, timeline } from '../bundle/selectors'; import { Badge, Card, Empty, Metric, Table } from '../components/ui'; import { DetectionChart } from '../components/DetectionChart';
const Page=({title,summary,children}:{title:string;summary:string;children:React.ReactNode})=><><header className="pageHead"><div><p className="eyebrow">Incident command / {title}</p><h1>{title}</h1><p>{summary}</p></div></header><div className="grid">{children}</div></>;
const value=(x:unknown)=>typeof x==='string'||typeof x==='number'?String(x):unavailable;
export function Overview({b}:{b:Bundle}){const h=lead(b), r=report(b), p=plan(b), rec=recovery(b), im=impact(b); return <Page title="Overview" summary="Deterministic synthetic incident records, presented read-only."><Card title="Lifecycle" wide><div className="stageList">{b.stages.map(s=><div key={s.key}><Badge value={s.status}/><b>{s.title}</b><small>{s.summary}</small></div>)}</div></Card><Card title="Diagnosis"><Metric label="Leading hypothesis" value={value(h?.title)}/><Metric label="Posterior probability" value={percent(h?.posterior_probability)}/><Metric label="Confidence" value={percent(r?.confidence)}/></Card><Card title="Governance"><Metric label="Policy outcome" value={value((p?.policy_decision as RecordValue|undefined)?.outcome)}/><Metric label="Execution" value={value(receipt(b)?.status)}/><Metric label="Recovery" value={value(rec?.decision)}/></Card><Card title="Measured impact"><Metric label="Affected customers" value={integer(im?.exposed_customers)}/><Metric label="Immediate revenue effect" value={currency(im?.immediate_revenue_loss,im?.currency)}/><Metric label="Future revenue at risk" value={currency(im?.future_revenue_at_risk,im?.currency)}/></Card><Card title="Public-bundle integrity"><Metric label="Schema" value={b.schema_version}/><Metric label="Files" value={integer(b.files.length)}/><Metric label="Mode" value="Read-only" detail="No production controls"/></Card></Page>}
export function Detection({b}:{b:Bundle}){const points=rows(b,'detectors'); return <Page title="Detection" summary="Exact exported detector observations; all timestamps are UTC."><Card title="Detector observations" wide>{points.length?<><DetectionChart points={points}/><p className="chartSummary">{points.length} exported observations. The table is the exact accessible alternative to the plotted rows.</p><Table head={['Period start','Metric','Cohort','Observed','Expected range','p / q','Anomaly']} rows={points.map(p=>[time(p.period_start),value(p.display_name),value(p.cohort_name),number(p.observed_value),`${number(p.expected_lower)} – ${number(p.expected_upper)}`,`${score(p.p_value)} / ${score(p.q_value)}`,value(p.is_anomaly)])}/></>:<Empty section="detector observations"/>}</Card><Card title="Anomaly events">{rows(b,'anomaly_events').length?<Table head={['Start','Metric','Severity']} rows={rows(b,'anomaly_events').map(x=>[time(x.started_at),value(x.metric_name),value(x.severity)])}/>:<Empty section="anomaly events"/>}</Card><Card title="Change points">{rows(b,'change_points').length?<Table head={['Detected','Metric','Direction']} rows={rows(b,'change_points').map(x=>[time(x.detected_at),value(x.metric_name),value(x.direction)])}/>:<Empty section="change-point events"/>}</Card></Page>}
export function Investigation({b}:{b:Bundle}){const r=report(b), hs=hypotheses(b);return <Page title="Investigation" summary="Posterior calculation and authorization are deterministic project responsibilities."><Card title="Decision state"><div className="metrics"><Metric label="Decision" value={value(r?.decision)}/><Metric label="Confidence" value={percent(r?.confidence)}/><Metric label="Normalized entropy" value={score(r?.normalized_entropy)}/><Metric label="Abstention" value={value(r?.abstained)}/></div></Card><Card title="Ranked hypotheses" wide>{hs.length?<div className="hypotheses">{hs.map((h,i)=><article key={String(h.hypothesis_id)}><b>{i+1}. {value(h.title)}</b><strong>{percent(h.posterior_probability)}</strong><p>{value(h.rationale)}</p><small>Prior {percent(h.prior_probability)} · support {asRows(h.evidence).length || (Array.isArray(h.supporting_evidence_ids)?h.supporting_evidence_ids.length:0)} · contradictions {Array.isArray(h.contradicting_evidence_ids)?h.contradicting_evidence_ids.length:0}</small><details><summary>Falsifiers and source references</summary><ul>{(Array.isArray(h.falsifiers)?h.falsifiers:[]).map(x=><li key={String(x)}>{String(x)}</li>)}</ul></details></article>)}</div>:<Empty section="ranked hypotheses"/>}</Card></Page>}
export function Evidence({b}:{b:Bundle}){const events=timeline(b);return <Page title="Evidence" summary="Source events sorted by their exported timestamps."><Card title="Operational timeline" wide>{events.length?<div className="timeline">{events.map((e,i)=><article key={String(e.timeline_event_id??i)}><i>{i+1}</i><div><Badge value={e.event_type}/><b>{value(e.title)}</b><small>{time(e.occurred_at)} · {value(e.source)}</small><p>{value(e.detail)}</p></div></article>)}</div>:<Empty section="operational timeline"/>}</Card></Page>}
export function Impact({b}:{b:Bundle}){const im=impact(b), segments=rows(b,'segments'), estimates=rows(b,'causal_estimates'); return <Page title="Impact" summary="Synthetic, deterministic impact estimates; unavailable fields are not inferred."><Card title="Financial and customer impact"><div className="metrics"><Metric label="Affected customers" value={integer(im?.exposed_customers)}/><Metric label="Failed interactions" value={integer(im?.immediate_failed_interactions)}/><Metric label="Revenue effect" value={currency(im?.immediate_revenue_loss,im?.currency)}/><Metric label="Future revenue risk" value={currency(im?.future_revenue_at_risk,im?.currency)}/><Metric label="Support & recovery cost" value={currency(im?.support_and_recovery_cost,im?.currency)}/><Metric label="Churn exposure" value={percent(im?.incremental_churn_rate)}/></div></Card><Card title="Segments" wide>{segments.length?<Table head={['Segment','Customers','Exposed','Churn','Revenue risk']} rows={segments.map(x=>[`${value(x.segment_name)}: ${value(x.segment_value)}`,integer(x.customers),integer(x.exposed_customers),percent(x.weighted_incremental_churn),currency(x.revenue_at_risk,im?.currency)])}/>:<Empty section="segment impact"/>}</Card><Card title="Causal diagnostics" wide>{estimates.length?<Table head={['Estimator','Estimand','Estimate','95% interval','Sample']} rows={estimates.map(x=>[value(x.estimator),value(x.estimand),number(x.estimate),`${number(x.lower_ci)} – ${number(x.upper_ci)}`,integer(x.sample_size)])}/>:<Empty section="causal estimates"/>}</Card></Page>}
export function Remediation({b}:{b:Bundle}){const p=plan(b), x=receipt(b), rec=recovery(b); const metrics=asRows(rec?.metric_evaluations);return <Page title="Remediation & Recovery" summary="Historical governed records only; this page cannot execute a change."><Card title="Policy and simulated execution"><p>{typeof p?.summary==='string'?p.summary:unavailable}</p><div className="metrics"><Metric label="Risk" value={value(p?.risk_level)}/><Metric label="Policy" value={value((p?.policy_decision as RecordValue|undefined)?.outcome)}/><Metric label="Required approvals" value={integer(p?.required_approvals)}/><Metric label="Execution state" value={value(x?.status)}/></div></Card><Card title="Recovery verification" wide>{rec?<><Badge value={rec.decision}/><div className="recovery">{metrics.map(m=><div key={value(m.metric_id)}><b>{value(m.metric_id)}</b><span>{value(m.status)}</span><small>equivalence p {score(m.equivalence_pvalue)} · sustained {integer(m.sustain_count)} periods</small></div>)}</div></>:<Empty section="recovery report"/>}</Card></Page>}
export function Evaluation({b}:{b:Bundle}){const e=fileMetric(b);const bins=asRows(e?.reliability_bins).filter(x=>Number(x.count)>0);return <Page title="Evaluation" summary="Synthetic benchmark measures, grouped by their stated semantics."><Card title="Diagnosis and calibration"><div className="metrics"><Metric label="Top-1 accuracy" value={percent(e?.top1_accuracy)}/><Metric label="Top-3 recall" value={percent(e?.top3_recall)}/><Metric label="Reciprocal rank" value={score(e?.mean_reciprocal_rank)}/><Metric label="Brier score" value={score(e?.brier_score)}/><Metric label="Log loss" value={score(e?.clipped_log_loss)}/><Metric label="ECE" value={percent(e?.expected_calibration_error)}/><Metric label="Coverage" value={percent(e?.coverage)}/><Metric label="Selective risk" value={percent(e?.selective_risk)}/></div></Card><Card title="Reliability bins" wide>{bins.length?<Table head={['Range','Cases','Confidence','Accuracy']} rows={bins.map(x=>[`${percent(x.lower_bound)} – ${percent(x.upper_bound)}`,integer(x.count),percent(x.mean_confidence),percent(x.accuracy)])}/>:<Empty section="reliability bins"/>}</Card><Card title="Safety"><Metric label="Authority violations" value={integer(e?.prohibited_action_authorized_count)}/><Metric label="Unsupported claims" value={integer(e?.unsupported_claim_count)}/></Card></Page>};
const fileMetric=(b:Bundle)=>{const f=b.files.find(x=>x.path.endsWith('evaluation-smoke/aggregate-metrics.json'))?.content;return f&&typeof f==='object'&&!Array.isArray(f)?f as RecordValue:undefined};
export function System({b,sourceCommit,integrity}:{b:Bundle;sourceCommit?:string;integrity:'verified'|'warning'|'local'}){return <Page title="System & Limitations" summary="Static presentation over a validated public artifact."><Card title="Architecture" wide><div className="architecture"><div>Deterministic exporter</div><span>→</span><div>Validated public bundle</div><span>→</span><div>Read-only dashboard</div></div><p>{b.disclaimer}</p></Card><Card title="Identity"><Metric label="Bundle kind" value={b.bundle_kind}/><Metric label="Schema" value={b.schema_version}/><Metric label="Bundle source commit" value={b.source_commit??unavailable}/><Metric label="Deployed source commit" value={sourceCommit??'Local development: deployment identity unavailable'}/><Metric label="Integrity" value={integrity}/></Card><Card title="Authority boundary"><p>No browser authority for approval, remediation, execution, rollback, recovery, evaluator decisions, credentials, filesystem access, or arbitrary network retrieval.</p></Card></Page>}
